package com.fpt.framework.data.dql.parser;

public enum TokenType {
	LPAREN, RPAREN, LBRACK, RBRACK, COMMA, EQ, NEQ,

	STRING, NUMBER, DATE_LITERAL, DATETIME_LITERAL, TRUE, FALSE, NULL,

	IDENT,

	AND, OR, NOT, IN, LIKE, CONTAINS, STARTS_WITH, ENDS_WITH, IGNORECASE,

	VAR,

	GT, GTE, LT, LTE,

	IS, EXISTS,
	ORDER, BY, ASC, DESC, SELECT, DISTINCT
}
